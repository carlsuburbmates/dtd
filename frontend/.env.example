# Current frontend runtime requirement
REACT_APP_BACKEND_URL=http://localhost:8001

# Optional historical/deferred provider keys.
# These are not required by the current frontend code path unless code starts consuming them again.
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com
NEXT_PUBLIC_SENTRY_DSN=
NEXT_PUBLIC_SENTRY_ENVIRONMENT=development
NEXT_PUBLIC_SENTRY_TRACES_SAMPLE_RATE=0.1

# Optional future auth migration (disabled in current lock state)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
