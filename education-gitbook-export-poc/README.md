# The First Leash GitBook Export POC

This folder is a GitBook-ready markdown export plan.

- `docs/README.md` is the course home
- `docs/SUMMARY.md` defines the navigation order
- `docs/modules/...` contains module, lesson, and tool pages
- `.gitbook.yaml` points GitBook Git Sync at the `docs/` root
